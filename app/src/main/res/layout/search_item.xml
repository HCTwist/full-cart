<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground">

    <TextView
        android:id="@+id/search_item_dot"
        android:layout_width="@dimen/search_dot"
        android:layout_height="@dimen/search_dot"
        android:layout_marginStart="@dimen/margin"
        android:layout_marginTop="@dimen/margin"
        android:layout_marginBottom="@dimen/margin"
        android:background="@drawable/dot"
        android:gravity="center"
        android:textSize="@dimen/small_font"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/search_item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin"
        android:layout_marginEnd="@dimen/margin"
        android:ellipsize="end"
        android:lines="1"
        android:textColor="@color/body"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/search_item_quick_button"
        app:layout_constraintStart_toEndOf="@id/search_item_dot"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/search_item_quick_button"
        android:layout_width="@dimen/list_action_size"
        android:layout_height="@dimen/list_action_size"
        android:layout_marginEnd="@dimen/margin"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/item_search_quick_add_cd"
        android:minWidth="0dp"
        android:minHeight="0dp"
        android:padding="@dimen/list_action_padding"
        android:scaleType="fitCenter"
        android:src="@drawable/outline_add_circle_outline_24"
        android:tint="@color/body_secondary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>